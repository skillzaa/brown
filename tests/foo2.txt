       if path.is_file(){
                  let e = path.extension().unwrap();
                  let e_str = e.to_str().unwrap();
                  if e_str == ext                    {
                    v.push(entry)
                  }
                }