use brown as bro;
mod setup;

#[test]
fn get_dir_test() {
    //remove old remanants 
    let _ = setup::tear_down();
    // assert!(del_old.is_ok());
    let bup  = setup::build_up_dirs();
    assert!(bup.is_ok()); 
// get the parent folder
//    let a = bro::get_le 
// pub fn run(){
//     let finished = false;
//     let new_level:Vec<DirEntry> = Vec::new();
//     loop {
    
    
//     if finished == true {break;}
//     }// the main loop outer most    
    
//     }
    
    let _ = setup::tear_down();        

}
